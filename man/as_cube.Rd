% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/export.R
\name{as_cube}
\alias{as_cube}
\alias{as_cube.sf}
\title{Export AlphaEarth tiles to a data cube format.}
\usage{
as_cube(x, to = c("sits", "stars"), ...)

\method{as_cube}{sf}(x, to = c("sits", "stars"), ...)
}
\arguments{
\item{x}{An \code{sf} object returned by \code{\link[=search]{search()}}.}

\item{to}{Character naming the target backend, either \code{"sits"} or \code{"stars"}.}

\item{...}{Extra arguments forwarded to the selected backend (see Details).}
}
\value{
The object produced by the selected format.
}
\description{
Turns the result of \code{\link[=search]{search()}} into a data cube.
}
\details{
The backends accept the following arguments through \code{...}:
\itemize{
\item \code{"sits"}: \code{output_dir} (directory for the per-band VRTs), \code{bands} (embedding
bands, \code{A00}-\code{A63}, defaults to all 64), \code{roi} (optional crop, any object
accepted by \code{\link[=search]{search()}}), and any further argument passed on to
\code{sits::sits_cube()} (e.g. \code{multicores}, \code{progress}). Returns a \code{sits} cube.
\item \code{"stars"}: \code{output_dir} (directory for the intermediate VRTs, defaults to a
temporary directory), \code{bands}, \code{roi}, \code{proxy} (read lazily as a
\code{stars_proxy}, defaults to \code{TRUE}), and any further argument passed on to
\code{\link[stars:read_stars]{stars::read_stars()}}. Returns one \code{stars} object for a single tile, or a
named list of \code{stars} objects keyed by tile id.
}
}
\note{
To materialise the tiles as GeoTIFFs on disk instead, use \code{\link[=download]{download()}}.
}
\examples{
\dontrun{
  tiles <- alphaearth::search(
    roi = my_roi,
    start_date = "2020",
    end_date = "2021"
  )

  # read the tiles as a stars cube
  cube <- alphaearth::as_cube(
    x = tiles,
    to = "stars",
    bands = c("A00", "A01")
  )

  # read the tiles as a sits cube
  cube <- alphaearth::as_cube(
    x = tiles,
    to = "sits",
    bands = c("A00", "A01")
  )
}
}
\seealso{
\code{\link[=search]{search()}}, \code{\link[=download]{download()}}
}
