% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/search.R
\name{search}
\alias{search}
\title{Search AlphaEarth tiles}
\usage{
search(roi = NULL, start_date = NULL, end_date = NULL, fid = NULL)
}
\arguments{
\item{roi}{object with the spatial region of interest (more information below).}

\item{start_date}{character with the start date of the temporal filter (more information below).}

\item{end_date}{character with the end date of the temporal filter (more information below).}

\item{fid}{Integer vector of feature ids to select directly.}
}
\value{
An \code{sf} object (with class \code{alphaearth_tiles}) with one row per
tile, including \code{fid}, \code{tile}, \code{year}, \code{date}, \code{crs}, \code{utm_zone}, the
\code{tiff_url}/\code{vrt_url} of the tile, its WGS84 bounds and a \code{geom} (\code{sfc})
column.
}
\description{
Queries the local index built by \code{\link[=index]{index()}} and returns the matching tiles.
}
\note{
The \code{roi} argument accepts the following types of objects:
\itemize{
\item an \code{sf}/\code{sfc} object,
\item an \code{sf} \code{bbox},
\item a named numeric vector \code{c(xmin, ymin, xmax, ymax)} in EPSG:4326,
\item a \code{terra} \code{SpatRaster}/\code{SpatVector} (its extent is used).
}

Any CRS is accepted and reprojected to EPSG:4326.

The \code{date} argument accepts the following formats:
\itemize{
\item "YYYY" (e.g. "2020"),
\item "YYYY-MM-DD" (e.g. "2020-01-01").
}
}
\examples{
\dontrun{
alphaearth::search(
  roi = c(xmin = -47.9, ymin = -15.9, xmax = -47.8, ymax = -15.8),
  start_date = "2020", end_date = "2020"
)
}
}
\seealso{
\code{\link[=index]{index()}}, \code{\link[=as_sits]{as_sits()}}
}
