% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/vrt.R
\name{as_vrt}
\alias{as_vrt}
\title{Write AlphaEarth tiles as virtual rasters (VRT)}
\usage{
as_vrt(
  x,
  output_dir,
  bands = NULL,
  roi = NULL,
  layout = c("stack", "bands"),
  overwrite = FALSE,
  progress = TRUE,
  multicores = 1L
)
}
\arguments{
\item{x}{An \code{sf} object returned by \code{\link[=search]{search()}}.}

\item{output_dir}{Directory where the VRTs are written (created if needed).}

\item{bands}{Character vector of embedding bands to reference, from \code{A00} to
\code{A63} (defaults to all 64 bands).}

\item{roi}{Optional region of interest used to crop each tile. Defaults to \code{NULL} (full tiles).}

\item{layout}{One of \code{"stack"} (one multi-band VRT per tile) or \code{"bands"} (one VRT per band).}

\item{overwrite}{Logical. Re-write files that already exist. Defaults to \code{FALSE}.}

\item{progress}{Logical. Show a progress bar. Defaults to \code{TRUE}.}

\item{multicores}{Integer. Number of parallel workers used to write the files.
Defaults to \code{1} (sequential). Values above \code{1} write tiles concurrently.}
}
\value{
A \code{tibble} (invisible) with one row per written file.
}
\description{
Materialises the selected tiles as lightweight GDAL VRT files
that point at the remote COGs (via \verb{/vsicurl/}). Two on-disk formats
are supported for VRTs:
\itemize{
\item \code{"stack"}: one multi-band VRT per tile, referencing every requested band
(\verb{ae_<tile>_<year>.vrt});
\item \code{"bands"}: one single-band VRT per band (\verb{ae_<tile>_<band>_<year>.vrt}).
}

Each tile keeps its native UTM CRS (no reprojection). Existing files are reused
unless \code{overwrite = TRUE}.
}
\examples{
\dontrun{
  tiles <- alphaearth::search(
    roi = my_roi,
    start_date = "2020",
    end_date   = "2020"
  )

  # one multi-band VRT per tile
  vrts <- alphaearth::as_vrt(tiles, output_dir = "embeddings")

  # one VRT per embedding dimension
  vrts <- alphaearth::as_vrt(
    x = tiles,
    output_dir = "embeddings",
    bands = c("A00", "A01"),
    layout = "bands"
  )
}
}
\seealso{
\code{\link[=search]{search()}}, \code{\link[=download]{download()}}, \code{\link[=as_cube]{as_cube()}}
}
