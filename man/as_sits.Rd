% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sits.R
\name{as_sits}
\alias{as_sits}
\alias{as_sits.sf}
\title{Prepare AlphaEarth tiles as a local sits cube}
\usage{
as_sits(x, ...)

\method{as_sits}{sf}(x, output_dir, bands = NULL, ...)
}
\arguments{
\item{x}{An \code{sf} object returned by \code{\link[=search]{search()}}, with at least the \code{tile},
\code{vrt_url} and \code{date} columns.}

\item{...}{Extra arguments passed to \code{sits::sits_cube()} (e.g.\code{multicores}, \code{progress}).}

\item{output_dir}{Directory where the cube vrt files are written (created if needed).}

\item{bands}{Character vector of embedding bands to write, from \code{A00} to \code{A63} (Defaults to all 64 bands).}
}
\value{
The \code{sits} cube returned by \code{sits::sits_cube()}.
}
\description{
Turns the result of \code{\link[=search]{search()}} into a local data cube usable
by the \code{sits} package. For each selected tile the corrected.
}
\examples{
\dontrun{
  # search for tiles
  tiles <- alphaearth::search(
    roi = my_roi,
    start_date = "2020",
    end_date   = "2021"
  )

  # build the data cube
  cube <- alphaearth::as_sits(
    x = tiles,
    output_dir = tempdir(),
    multicores = 4,
    progress = TRUE
  )
}
}
\seealso{
\code{\link[=search]{search()}}
}
