% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/download.R
\name{download}
\alias{download}
\title{Download AlphaEarth tiles as local GeoTIFFs}
\usage{
download(
  x,
  output_dir,
  bands = NULL,
  roi = NULL,
  layout = c("stack", "bands"),
  overwrite = FALSE,
  progress = TRUE,
  multicores = 1L
)
}
\arguments{
\item{x}{An \code{sf} object returned by \code{\link[=search]{search()}}.}

\item{output_dir}{Directory where the GeoTIFFs are written (created if needed).}

\item{bands}{Character vector of embedding bands to download, from \code{A00} to
\code{A63} (defaults to all 64 bands).}

\item{roi}{Optional region of interest used to crop each tile (any object
accepted by \code{\link[=search]{search()}}). Defaults to \code{NULL} (full tiles).}

\item{layout}{One of \code{"stack"} (one multi-band file per tile) or \code{"bands"}
(one file per embedding dimension).}

\item{overwrite}{Logical. Re-write files that already exist. Defaults to \code{FALSE}.}

\item{progress}{Logical. Show a progress bar. Defaults to \code{TRUE}.}

\item{multicores}{Integer. Number of parallel workers used to write the files.
Defaults to \code{1} (sequential); values above \code{1} download
tiles concurrently.}
}
\value{
A \code{tibble} (invisible) with one row per written file.
}
\description{
Materialises the selected tiles into local GeoTIFFs, and
(optionally) cropping them to a region of interest.

Two on-disk layouts are supported:
\itemize{
\item \code{"stack"}: one multi-band GeoTIFF per tile, holding every requested
embedding dimension (\verb{ae_<tile>_<year>.tif});
\item \code{"bands"}: one single-band GeoTIFF per embedding dimension
(\verb{ae_<tile>_<band>_<year>.tif}).
}

Each tile keeps its native UTM CRS (no reprojection). Existing files are
reused unless \code{overwrite = TRUE}.
}
\examples{
\dontrun{
  tiles <- alphaearth::search(
    roi = my_roi,
    start_date = "2020",
    end_date   = "2020"
  )

  # one multi-band GeoTIFF per tile, cropped to a ROI
  files <- alphaearth::download(
    x = tiles,
    output_dir = "embeddings",
    roi = c(
      xmin = -47.9,
      ymin = -15.9,
      xmax = -47.85,
      ymax = -15.85
    )
  )

  # one file per embedding dimension
  files <- alphaearth::download(
    x = tiles,
    output_dir = "embeddings",
    bands = c("A00", "A01"),
    layout = "bands"
  )
}
}
\seealso{
\code{\link[=search]{search()}}, \code{\link[=as_vrt]{as_vrt()}}, \code{\link[=as_cube]{as_cube()}}
}
